class BLETimer {
  static const scanDelay = Duration(seconds: 5);
  static const connectedDelay = Duration(seconds: 2);
}

class MQTTtimer {
  static const mqttConnectedDelay = Duration(seconds: 1);
}
